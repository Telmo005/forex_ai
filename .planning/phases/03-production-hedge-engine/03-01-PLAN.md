---
phase: 03-production-hedge-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hedge_engine.py
  - tests/test_hedge_engine.py
autonomous: true
requirements:
  - HEDGE-01
  - HEDGE-03
user_setup: []

must_haves:
  truths:
    - "load_eligible_strategies() returns only rows where status=='passed' AND wf_passed==1 AND revalidated_on_real_data==1 (D-07/HEDGE-01)"
    - "A strategy that passed walk-forward on synthetic data only (wf_passed==1, revalidated_on_real_data==0) is NEVER returned as eligible (Pitfall 4)"
    - "evaluate_hedge_signal() opens a hedge only when is_cointegrated AND abs(zscore)>=2.0 AND correlation>=0.5 (D-01/D-02)"
    - "When an open position has correlation below MIN_CORRELATION_EXIT AND z-score simultaneously within EXIT_THRESHOLD, the returned exit trigger is 'correlation_breakdown', never 'reversion' (D-04/D-05, Pitfall 2)"
    - "recheck_cointegration() returns is_cointegrated=False on ANY coint() exception (fail-closed, HEDGE-03 security framing)"
    - "Two approved strategies targeting the identical pair resolve via an explicit, logged tie-break, never a silent strategies[0] (D-08, Pitfall 3)"
  artifacts:
    - "src/hedge_engine.py (module docstring, HEDGE_PARAMS, COINT_RECHECK_EVERY_BARS, STOP_DISTANCE_STD_MULTIPLIER, load_eligible_strategies, select_strategy_for_pair, evaluate_hedge_signal, recheck_cointegration)"
    - "tests/test_hedge_engine.py (pure-function assertions for HEDGE-01/HEDGE-03 and Pitfalls 2/3/4/6)"
  key_links:
    - "load_eligible_strategies -> strategy_registry.list_strategies(status='passed') (never a raw sqlite3.connect)"
    - "recheck_cointegration -> statsmodels.tsa.stattools.coint (same call pattern as data_pipeline.scan_hedge_candidates)"
    - "HEDGE_PARAMS constants each cite their D-xx decision id inline"
---

<objective>
Build the pure decision core of `src/hedge_engine.py`: the named-constant table, the strategy-eligibility loader (HEDGE-01), the per-pair entry/exit signal evaluator, and the cointegration re-verification function (HEDGE-03). These are all pure, side-effect-free functions with fully defined inputs/outputs — the testable heart of the phase. The live-like loop and the risk-engine handoff are built on top of these in Plan 03-02.

Purpose: HEDGE-01 (only dashboard-approved + real-data-revalidated strategies are eligible) and HEDGE-03 (cointegration is continuously re-verified, never treated as permanent) live entirely in these pure functions. Getting them right and test-locked first means Plan 03-02's loop is pure composition.
Output: `src/hedge_engine.py` (constants + three pure functions + tie-break helper) and `tests/test_hedge_engine.py` encoding the eligibility gate, the exit-ordering override, the fail-closed cointegration behavior, and the tie-break.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-hedge-engine/03-CONTEXT.md
@.planning/phases/03-production-hedge-engine/03-RESEARCH.md
@.planning/phases/03-production-hedge-engine/03-PATTERNS.md
@docs/hedge_engine_spec.md
@.claude/skills/quant-finance-math/SKILL.md

# Analog sources — read to mirror conventions, do NOT reimplement their logic
@src/risk_limits.py
@src/risk_engine.py
@src/strategy_registry.py
@src/data_pipeline.py
@src/backtest_engine.py
@tests/test_risk_engine.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Module scaffold, named-constant table, and strategy-eligibility loader (HEDGE-01)</name>
  <files>src/hedge_engine.py, tests/test_hedge_engine.py</files>
  <behavior>
    - load_eligible_strategies returns [] when the DB has no rows / does not exist (mirror list_strategies empty-df guard).
    - Given three registry rows — (A: status=passed, wf_passed=1, revalidated_on_real_data=1), (B: status=passed, wf_passed=1, revalidated_on_real_data=0), (C: status=failed, wf_passed=1, revalidated_on_real_data=1) — only row A is returned.
    - A row with wf_passed=1 but revalidated_on_real_data=0 is excluded AND an explicit warning is logged naming the excluded id (two-flag-never-inferred, Pitfall 4).
    - select_strategy_for_pair given two eligible rows for the identical (pair_a, pair_b) returns exactly one deterministically (most-recently-approved by created_at wins) AND logs a warning naming both ids (D-08, Pitfall 3) — never a silent first-match.
  </behavior>
  <action>
    Create `src/hedge_engine.py` with a module docstring mirroring `src/risk_limits.py`'s framing (03-PATTERNS.md lines 24-40): state which requirements it satisfies (HEDGE-01/02/03), which decision ids each constant traces to (D-01..D-08, 03-CONTEXT.md), and the structural non-negotiable "this module never sizes a position and never rejects an order for a risk reason — every proposal is routed through risk_engine.evaluate_order() (see HEDGE-02, implemented in Plan 03-02)."

    Define the named-constant table exactly per 03-PATTERNS.md lines 56-64: `HEDGE_PARAMS` as a plain dict with keys ENTRY_THRESHOLD=2.0 (D-01), MIN_CORRELATION_ENTRY=0.5 (D-02), EXIT_THRESHOLD=0.3 (D-03), MIN_CORRELATION_EXIT=0.4 (D-04), MAX_HOLD_BARS=75 (D-05), each with its D-id as a trailing inline comment; module-level COINT_RECHECK_EVERY_BARS=50 (D-06); and STOP_DISTANCE_STD_MULTIPLIER (Claude's discretion per RESEARCH.md Open Question 1 / Assumption A1 — choose a documented spread-std-dev multiplier, e.g. 2.0, in "R" units consistent with backtest_engine.py; this is consumed in Plan 03-02's risk handoff but MUST be defined here as a named, documented constant, not an inline literal). Add a deliberate-comparison-operator note (mirror risk_engine.py module docstring lines 40-51) documenting the `>=` on entry, `<=` on reversion exit, and strict `<` on correlation-breakdown exit choices so no boundary-inclusivity is left implicit.

    Add a module logger `log = logging.getLogger("hedge_engine")` per project convention.

    Implement `load_eligible_strategies(db_path: str) -> list[dict]` per HEDGE-01/D-07 and RESEARCH.md's verified Strategy Loading Contract: call `strategy_registry.list_strategies(db_path, status="passed")` — use the literal DB string "passed", NOT the UI display label (the emoji approval label is a dashboard-only display mapping and matches ZERO rows in the database). Then filter the returned DataFrame for `wf_passed == 1` AND `revalidated_on_real_data == 1` using the explicit `bool(record.get(...))` + explicit AND style copied from risk_engine.resolve_kelly_inputs (03-PATTERNS.md lines 222-233); never infer one flag from the other. Log an explicit warning (naming the excluded id) whenever a row is excluded solely because one of the two flags is 0, matching resolve_kelly_inputs's warning-on-fallback discipline. Return `.to_dict("records")`. Never open a raw sqlite3 connection in this module (Don't-Hand-Roll, RESEARCH.md).

    Implement `select_strategy_for_pair(eligible: list[dict], pair_a: str, pair_b: str) -> dict | None` implementing D-08's explicit tie-break: filter eligible rows to those matching (pair_a, pair_b); if none, return None; if exactly one, return it; if more than one, pick the most-recently-approved (max `created_at`) AND `log.warning` naming ALL contending ids and the chosen id. This is the named, tested code path that replaces any silent `strategies[0]`.

    Create `tests/test_hedge_engine.py` opening with a suite-framing docstring (mirror tests/test_risk_engine.py lines 1-28): name which HEDGE-* requirements and which 03-RESEARCH.md Pitfalls this suite proves as executable tests. Use the `_healthy_*()`/`_valid_*(**overrides)` baseline-fixture-with-override style (03-PATTERNS.md lines 266-281). Add a fixture that builds a temporary strategy_lab.db (via strategy_registry.init_db + save_strategy + save_walk_forward_result) so the eligibility filter is tested against the REAL schema, not a mock. Encode the four behaviors above as named tests, isolating one D-id / Pitfall per test.
  </action>
  <verify>
    <automated>python -m pytest tests/test_hedge_engine.py -q -k "eligib or tie_break or two_flag"</automated>
  </verify>
  <done>load_eligible_strategies filters on the literal "passed" string plus both walk-forward flags with a warning on single-flag exclusion; select_strategy_for_pair resolves same-pair duplicates via most-recently-approved with a warning naming both ids; all named tests pass against a real temporary strategy_lab.db.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Entry/exit signal evaluator with correlation-breakdown-first exit ordering (D-01..D-05)</name>
  <files>src/hedge_engine.py, tests/test_hedge_engine.py</files>
  <behavior>
    - Flat (position is None): returns an open_hedge dict ONLY when is_cointegrated AND abs(zscore)>=ENTRY_THRESHOLD AND correlation>=MIN_CORRELATION_ENTRY; else None.
    - Entry direction: zscore>0 (spread above mean) -> direction=-1 (short spread); zscore<0 -> direction=+1 (long spread).
    - Entry with correlation>=0.5 but is_cointegrated=False returns None (Pitfall 6: correlation alone is never sufficient).
    - Open position: correlation < MIN_CORRELATION_EXIT returns close_hedge with trigger 'correlation_breakdown' EVEN WHEN abs(zscore) is also <= EXIT_THRESHOLD in the same call (D-05 override, Pitfall 2) — correlation_breakdown is checked first.
    - Open position: abs(zscore)<=EXIT_THRESHOLD (correlation still healthy) returns close_hedge trigger 'reversion'.
    - Open position: bars_held>=MAX_HOLD_BARS with correlation healthy and z-score not yet reverted returns close_hedge trigger 'time_stop'.
    - A position held past MAX_HOLD_BARS whose correlation has ALSO broken down returns trigger 'correlation_breakdown', not 'time_stop' (breakdown priority holds regardless of hold time, D-05 note).
    - Open position with no exit condition met returns None.
  </behavior>
  <action>
    Implement `evaluate_hedge_signal(pair_a, pair_b, zscore, correlation, is_cointegrated, position, params, bars_held=0) -> dict | None` in `src/hedge_engine.py` following the fixed-order, short-circuit, explicit-reason pattern of risk_engine.evaluate_order (03-PATTERNS.md lines 92-117). Use a local `_close(trigger, value)` helper (mirroring risk_engine's `_reject`) to build the close_hedge dict and avoid per-branch boilerplate.

    Entry branch (position is None): require ALL of is_cointegrated, abs(zscore) >= params["ENTRY_THRESHOLD"], correlation >= params["MIN_CORRELATION_ENTRY"] (per D-01/D-02 and Pitfall 6 — cointegration is mandatory, never optional, never replaced by correlation). Return {"action":"open_hedge","pair_a","pair_b","direction","trigger":"spread_zscore","trigger_value":zscore} where direction = -1 if zscore>0 else 1. Otherwise return None.

    Exit branch (position not None) — the exit-check ORDER is load-bearing and MUST be: (1) correlation breakdown, (2) reversion, (3) time-stop. Check `correlation < params["MIN_CORRELATION_EXIT"]` FIRST and return trigger "correlation_breakdown"; this MUST take priority over reversion and time-stop per 03-CONTEXT.md D-05's explicit override ("MIN_CORRELATION_EXIT triggers independently and takes priority the moment correlation breaks, regardless of how long the position has been open") and RESEARCH.md Pitfall 2. Then check `abs(zscore) <= params["EXIT_THRESHOLD"]` -> trigger "reversion". Then check `bars_held >= params["MAX_HOLD_BARS"]` -> trigger "time_stop". Return None if none fire. Do NOT follow the literal ordering in docs/hedge_engine_spec.md (reversion-first) — that ordering is superseded by D-05; document this override in the function docstring.

    Every returned dict variant must be documented in the docstring with the D-id it enforces (mirror risk_engine.py's per-branch return-shape documentation, 03-PATTERNS.md lines 74-90). This module must NOT compute a lot size, an sl_price, or any risk gate here — it only emits the abstract open/close proposal (HEDGE-02 boundary).

    Add named tests to tests/test_hedge_engine.py for every behavior above. The correlation-breakdown-priority test is mandatory and must be named explicitly (RESEARCH.md Pitfall 2): construct a position where correlation < MIN_CORRELATION_EXIT AND abs(zscore) <= EXIT_THRESHOLD simultaneously, assert the returned trigger == "correlation_breakdown". Add the second override test: bars_held >= MAX_HOLD_BARS AND correlation broken -> trigger == "correlation_breakdown", not "time_stop".
  </action>
  <verify>
    <automated>python -m pytest tests/test_hedge_engine.py -q -k "entry or exit or breakdown or reversion or time_stop"</automated>
  </verify>
  <done>evaluate_hedge_signal gates entry on cointegration+z-score+correlation together, evaluates exits in correlation-breakdown -> reversion -> time-stop order, and the two correlation-breakdown-priority tests pass; the function computes no size/sl_price/risk gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cointegration re-verification, fail-closed (HEDGE-03)</name>
  <files>src/hedge_engine.py, tests/test_hedge_engine.py</files>
  <behavior>
    - recheck_cointegration on a genuinely cointegrated trailing window returns (True, pvalue) with pvalue < threshold.
    - recheck_cointegration on a non-cointegrated window returns (False, pvalue) with pvalue >= threshold.
    - recheck_cointegration when coint() raises (pathological series) returns (False, <sentinel/NaN>) — fail-closed, never (True, ...) and never a raised exception (HEDGE-03 security framing).
    - Uses only the trailing `coint_window` bars of the passed series (causal; no lookahead).
  </behavior>
  <action>
    Implement `recheck_cointegration(price_a: pd.Series, price_b: pd.Series, coint_window: int, pvalue_threshold: float) -> tuple[bool, float]` in `src/hedge_engine.py`, reusing the EXACT coint() call pattern from data_pipeline.scan_hedge_candidates (03-PATTERNS.md lines 134-151): `from statsmodels.tsa.stattools import coint`; slice the trailing `coint_window` bars (`price_a.iloc[-coint_window:]`, same for b) and reset index; call `score, pvalue, _ = coint(a, b)`; return `(pvalue < pvalue_threshold, pvalue)`. Do NOT reimplement Engle-Granger and do NOT reuse hedge_candidates.csv's stale is_cointegrated value (Pitfall 1 / Don't-Hand-Roll).

    Wrap the coint() call in `try/except Exception`. CRITICAL divergence from scan_hedge_candidates: that function does `continue` (silently omits the row) on exception; recheck_cointegration must instead return `(False, float("nan"))` and `log.warning` the pair + exception. An open position's cointegration check has no "just skip it" option — an exception must produce a negative, actionable result so the caller (Plan 03-02's loop) treats the pair as no-longer-cointegrated and can withhold/exit, never leaving stale state in place (RESEARCH.md Security Domain "fail closed"; 03-PATTERNS.md "Fail-closed on statistical-test exception").

    Default `coint_window` and `pvalue_threshold` to match data_pipeline's PipelineConfig conventions (coint_window default 1000, pvalue_threshold 0.05) so entry-time and re-test-time cointegration checks stay consistent; expose them as parameters (not hard-coded) so the loop can pass configured values.

    Add named tests: a cointegrated-window case (synthesize a stationary spread), a non-cointegrated-window case (two independent random walks), and the fail-closed case (pass a degenerate series that makes coint() raise — e.g. all-constant or too-short after slicing — and assert the result is (False, ...) with no exception propagated).
  </action>
  <verify>
    <automated>python -m pytest tests/test_hedge_engine.py -q -k "coint"</automated>
  </verify>
  <done>recheck_cointegration reuses statsmodels coint() on the trailing window, returns (True/False, pvalue) correctly, and returns (False, nan) — never raises and never returns True — on any coint() exception; tests for all three cases pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strategy_registry DB -> hedge_engine | `params` column is JSON text and numeric flags are read from SQLite; a malformed or internally-inconsistent row crosses into decision logic here |
| statsmodels coint() -> hedge_engine | a pathological price series can make the cointegration test raise mid-evaluation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Denial of Service | recheck_cointegration coint() call | high | mitigate | Wrap coint() in try/except; on any exception return (False, nan) and log — never crash the caller and never fail-open to True (Task 3) |
| T-03-02 | Tampering | load_eligible_strategies flag filter | high | mitigate | Require status=="passed" AND wf_passed==1 AND revalidated_on_real_data==1 with two-flag-never-inferred discipline; a synthetic-only (revalidated=0) strategy can never smuggle into eligibility (Task 1, Pitfall 4) |
| T-03-03 | Elevation of Privilege | select_strategy_for_pair tie-break | medium | mitigate | Explicit, logged, deterministic tie-break (most-recently-approved) prevents a silent first-match from promoting an unintended/duplicate param set (Task 1, D-08/Pitfall 3) |
| T-03-04 | Tampering | evaluate_hedge_signal entry gate | medium | mitigate | Entry requires is_cointegrated==True in addition to correlation; correlation alone can never open a position (Task 2, Pitfall 6) |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages installed this phase (RESEARCH.md Package Legitimacy Audit: not triggered); all deps pre-existing in requirements.txt |
</threat_model>

<verification>
- `python -m pytest tests/test_hedge_engine.py -q` — full Plan 03-01 suite passes.
- `python -c "import ast,sys; src=open('src/hedge_engine.py').read(); assert 'sqlite3' not in src, 'must not open raw sqlite3 — use strategy_registry'; print('ok: no raw sqlite3')"` — confirms the Don't-Hand-Roll DB-access rule.
- Grep confirms HEDGE_PARAMS keys ENTRY_THRESHOLD=2.0, MIN_CORRELATION_ENTRY=0.5, EXIT_THRESHOLD=0.3, MIN_CORRELATION_EXIT=0.4, MAX_HOLD_BARS=75 and COINT_RECHECK_EVERY_BARS=50 each cite a D-xx id.
</verification>

<success_criteria>
1. load_eligible_strategies returns only status=="passed" AND wf_passed==1 AND revalidated_on_real_data==1 rows; a synthetic-only-passed strategy is provably excluded (HEDGE-01).
2. evaluate_hedge_signal requires cointegration+z-score+correlation together on entry and evaluates exits correlation-breakdown-first (D-01..D-05, Pitfall 2/6).
3. recheck_cointegration reuses statsmodels coint() and fails closed to (False, nan) on any exception, never fail-open (HEDGE-03).
4. select_strategy_for_pair resolves same-pair duplicates via an explicit, logged tie-break (D-08).
5. No raw sqlite3 connection and no self-computed lot size / sl_price / risk gate in hedge_engine.py.
</success_criteria>

<output>
Create `.planning/phases/03-production-hedge-engine/03-01-SUMMARY.md` when done.
</output>
