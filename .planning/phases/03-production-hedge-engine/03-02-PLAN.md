---
phase: 03-production-hedge-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/hedge_engine.py
  - tests/test_hedge_engine.py
autonomous: true
requirements:
  - HEDGE-02
user_setup: []

must_haves:
  truths:
    - "Every open_hedge/close_hedge proposal is routed through risk_engine.evaluate_order() before it is anything more than a proposal (HEDGE-02)"
    - "hedge_engine.py never computes a lot size and never rejects an order for a risk reason itself — sizing/veto authority lives only in risk_engine.py"
    - "run_hedge_loop's body is identical for replay and (future) live feeds — only the feed iterator changes; there is no separate backtest-mode vs live-mode code path (Pitfall 5)"
    - "recheck_cointegration is invoked before every entry decision and every COINT_RECHECK_EVERY_BARS bars for an open position; a broken-down pair is withheld/exited, never trusted from a stale snapshot (HEDGE-03 wired end-to-end)"
    - "Kelly inputs for the risk handoff come from risk_engine.resolve_kelly_inputs(strategy_record), never invented in hedge_engine.py"
  artifacts:
    - "src/hedge_engine.py (Bar, replay_feed, propose_to_risk_engine, run_hedge_loop)"
    - "tests/test_hedge_engine.py (risk-handoff field-mapping test, no-self-sizing static test, feed/loop integration test)"
  key_links:
    - "propose_to_risk_engine -> risk_engine.evaluate_order (same-process Python call, OrderProposal field mapping)"
    - "run_hedge_loop -> evaluate_hedge_signal + recheck_cointegration + compute_rolling_beta/compute_zscore (composition of Plan 03-01 + backtest_engine functions)"
    - "replay_feed -> pd.read_parquet(output/features_*.parquet) yielding dict[symbol, Bar] (same shape a future live feed yields)"
---

<objective>
Compose Plan 03-01's pure decision functions into the running system: the risk-engine handoff shim that maps a hedge proposal onto risk_engine's OrderProposal and calls evaluate_order (HEDGE-02), a swappable parquet-replay feed, and the `run_hedge_loop()` driver that recalculates beta/z-score/correlation causally each bar, re-verifies cointegration on schedule (HEDGE-03 wired live), and passes every proposal through the risk engine.

Purpose: HEDGE-02 (propose, never execute — every order flows through risk_engine.evaluate_order) is realized here. The loop is the "live-like, replay-now / live-later without redesign" design the phase was explicitly scoped around: one loop body, a swappable feed.
Output: `src/hedge_engine.py` extended with `Bar`, `replay_feed`, `propose_to_risk_engine`, `run_hedge_loop`, and `tests/test_hedge_engine.py` extended with the risk-handoff field-mapping test, an executable no-self-sizing guard, and a feed-driven loop integration test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-hedge-engine/03-CONTEXT.md
@.planning/phases/03-production-hedge-engine/03-RESEARCH.md
@.planning/phases/03-production-hedge-engine/03-PATTERNS.md
@.planning/phases/03-production-hedge-engine/03-01-SUMMARY.md
@docs/hedge_engine_spec.md

# Analog sources — mirror conventions, do NOT reimplement
@src/risk_engine.py
@src/risk_limits.py
@src/backtest_engine.py
@src/data_pipeline.py
@tests/test_risk_engine.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Risk-engine handoff shim (HEDGE-02)</name>
  <files>src/hedge_engine.py, tests/test_hedge_engine.py</files>
  <behavior>
    - propose_to_risk_engine maps a hedge proposal dict {pair_a, pair_b, direction, ...} onto risk_engine.OrderProposal fields (symbol_a<-pair_a, symbol_b<-pair_b, direction<-direction, win_rate/payoff_ratio from caller, entry_price from caller, stop_distance_price_units from caller) and returns the RiskDecision from risk_engine.evaluate_order.
    - The returned object is a risk_engine.RiskDecision (approved bool + size_lots + sl_price + reject_reason), i.e. the sizing/approval decision was made by risk_engine, not by hedge_engine.
    - win_rate/payoff_ratio are obtained via risk_engine.resolve_kelly_inputs(strategy_record), never computed inside hedge_engine.
    - A malformed strategy params JSON blob is caught and the strategy skipped (logged), not crashing the caller (V5/DoS mitigation).
  </behavior>
  <action>
    Implement `propose_to_risk_engine(hedge_proposal, strategy_record, entry_price, stop_distance_price_units, account_state, correlation_matrix, new_position_exposure_pct=0.0) -> RiskDecision` in `src/hedge_engine.py`, using RESEARCH.md's verified Risk Engine Integration Contract (03-RESEARCH.md lines 378-409) and 03-PATTERNS.md "No Analog Found" note.

    Field mapping (the two shapes are NOT identical): construct `risk_engine.OrderProposal(symbol_a=hedge_proposal["pair_a"], symbol_b=hedge_proposal["pair_b"], win_rate=..., payoff_ratio=..., entry_price=entry_price, stop_distance_price_units=stop_distance_price_units, direction=hedge_proposal["direction"])`. Obtain (win_rate, payoff_ratio) by calling `risk_engine.resolve_kelly_inputs(strategy_record)` — hedge_engine must NOT invent Kelly inputs (HEDGE-02; RISK-09 lineage). Then call and return `risk_engine.evaluate_order(proposal, account_state, RiskLimits(), correlation_matrix, kill_switch_path=KILL_SWITCH_PATH, new_position_exposure_pct=new_position_exposure_pct)` (import RiskLimits and KILL_SWITCH_PATH from src.risk_limits).

    Compute stop_distance_price_units at the CALLER (the loop, Task 3) as `entry_spread_std * STOP_DISTANCE_STD_MULTIPLIER` (the named constant defined in Plan 03-01) — this function receives it as a parameter and does not invent it, matching risk_engine's OrderProposal docstring ("a distância do stop é decidida pela camada de hedge... este módulo não inventa a distância"). Before constructing OrderProposal, apply the V5 input-validation discipline (RESEARCH.md Security Domain): if any caller-supplied numeric (entry_price, stop_distance_price_units) is non-finite or non-positive, do NOT clamp to a fake-positive value — pass it through so risk_engine.validate_order_proposal's existing reject path fires (never bypass validation). Wrap any `json.loads` of the strategy `params` blob in try/except; on failure log and return a rejected-equivalent (skip the strategy), never crash the loop.

    hedge_engine must NOT open-code a lot-size formula, must NOT branch on exposure/drawdown itself, and must NOT import numeric risk fields from risk_limits.py to make its own gating decision — every risk decision is delegated to evaluate_order (HEDGE-02 boundary; Don't-Hand-Roll).

    Add a named test that constructs a hedge proposal + a real strategy_record + AccountState + correlation_matrix, calls propose_to_risk_engine, and asserts (a) the return is a RiskDecision, (b) the OrderProposal.symbol_a/symbol_b/direction were mapped from pair_a/pair_b/direction, (c) win_rate/payoff_ratio equal what resolve_kelly_inputs returns for that record. Add a malformed-params test asserting the loop-safe skip behavior.
  </action>
  <verify>
    <automated>python -m pytest tests/test_hedge_engine.py -q -k "risk or propose or handoff or kelly"</automated>
  </verify>
  <done>propose_to_risk_engine maps the hedge proposal onto OrderProposal, sources Kelly inputs via resolve_kelly_inputs, delegates the decision to risk_engine.evaluate_order, and never computes size/sl itself; tests confirm the mapping and the delegated RiskDecision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swappable parquet-replay feed + no-self-sizing static guard (HEDGE-02)</name>
  <files>src/hedge_engine.py, tests/test_hedge_engine.py</files>
  <behavior>
    - replay_feed yields one aligned cross-symbol dict[symbol, Bar] per step, oldest timestamp to newest, over the intersection of the per-symbol parquet indices.
    - replay_feed logs a warning if the index intersection drops more than a small fraction of bars from any symbol (data-alignment guard, RESEARCH.md Open Question 2).
    - Each yielded Bar carries (timestamp, symbol, close) read via explicit column access (no positional reliance).
    - A static test proves hedge_engine.py contains no open-coded lot-size formula and does not branch on risk_limits numeric fields for its own gating (HEDGE-02 structural, RISK-09-precedent style).
  </behavior>
  <action>
    Implement `Bar` (a `typing.NamedTuple` with timestamp/symbol/close) and `replay_feed(feature_parquet_paths: dict[str, str]) -> Iterator[dict[str, Bar]]` per RESEARCH.md Pattern 2 (03-RESEARCH.md lines 234-262) — this piece has no in-repo analog (03-PATTERNS.md "No Analog Found"), so Pattern 2's code is the primary reference; anchor only the `pd.read_parquet` and index-intersection mechanics to data_pipeline.load_all_symbols's existing shared-index convention. Read each symbol's `output/features_<SYMBOL>.parquet` with `pd.read_parquet`, intersect the DatetimeIndexes, iterate sorted timestamps, and yield `{sym: Bar(ts, sym, df.loc[ts, "close"]) for ...}` using explicit "close" column access. Log a warning when the intersected index drops more than a small documented fraction of any symbol's bars (weekend gaps / broker DST mismatch guard). Document that a future `live_feed()` (Phase 4, OUT OF SCOPE here) yields the identical dict[symbol, Bar] shape — the loop never learns which feed it consumes. Do NOT build the live feed adapter now (RESEARCH.md anti-pattern).

    Add an executable no-self-sizing guard to tests/test_hedge_engine.py (mirroring the RISK-09 executable-independence precedent in tests/test_risk_engine.py). Read src/hedge_engine.py source and assert, via string/AST inspection, that: it contains no lot-size arithmetic pattern that bypasses risk_engine (e.g. no local computation assigning a lots/size variable from equity), and it never references risk_limits numeric fields (max_pair_exposure_pct, kelly_fraction, etc.) to make a gating decision — only RiskLimits() is instantiated and handed to evaluate_order. Phrase the assertions by concept so the check does not self-invalidate; keep the forbidden-token list in the test body, not in hedge_engine.py comments.
  </action>
  <verify>
    <automated>python -m pytest tests/test_hedge_engine.py -q -k "feed or replay or self_sizing or independence"</automated>
  </verify>
  <done>replay_feed yields aligned dict[symbol, Bar] steps from parquet with an alignment-drop warning and a documented live-feed-compatible shape; the executable no-self-sizing guard passes, proving hedge_engine delegates all sizing/risk gating to risk_engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Live-like loop driver wiring entry/exit + scheduled cointegration re-check (HEDGE-02 + HEDGE-03 end-to-end)</name>
  <files>src/hedge_engine.py, tests/test_hedge_engine.py</files>
  <behavior>
    - run_hedge_loop consumes any feed iterator (replay now), maintains per-pair position state and bars-since-last-coint-check in function-local scope (no module globals).
    - Beta/z-score/correlation are recalculated each bar via backtest_engine.compute_rolling_beta/compute_zscore (reused verbatim, not reimplemented).
    - Cointegration is re-checked via recheck_cointegration once before every entry decision and every COINT_RECHECK_EVERY_BARS bars for an open position; a pair whose re-check returns is_cointegrated=False is withheld from entry, and an open position on a broken-down pair is exited via evaluate_hedge_signal's correlation/coint path — never trusting the original snapshot (HEDGE-03).
    - Every open_hedge/close_hedge proposal emitted by evaluate_hedge_signal is passed to the injected risk_evaluate_fn (== propose_to_risk_engine / risk_engine.evaluate_order) before being acted on; a risk-rejected open proposal does not open a position (HEDGE-02).
    - Running the loop over a fixed replay feed twice produces identical proposals (deterministic; no hidden state).
  </behavior>
  <action>
    Implement `run_hedge_loop(feed, eligible_strategies, risk_evaluate_fn, params=HEDGE_PARAMS, coint_recheck_every=COINT_RECHECK_EVERY_BARS, ...)` per RESEARCH.md Pattern 2 and 03-PATTERNS.md's run_hedge_backtest structural precedent (lines 174-195). Keep the params-dict-driven signature style (document expected keys in the docstring, matching run_hedge_backtest). Hold all mutable state (open positions per pair, bars_held per position, bars_since_coint_check per pair) in function-local variables — never module-level globals (risk_engine.py "no global state" rule, 03-PATTERNS.md lines 291-293).

    Loop body (identical for replay and future live — Pitfall 5: there is exactly ONE loop, no run_backtest/run_live fork): for each yielded bar dict, for each eligible strategy's pair — recalculate beta via `compute_rolling_beta` and z-score via `compute_zscore` (imported verbatim from backtest_engine, NOT reimplemented — 03-CONTEXT.md hard requirement, backtest/production drift anti-pattern) and the rolling correlation on the same causal window. Determine `is_cointegrated`: call `recheck_cointegration` before any new entry decision, and for an open position when `bars_since_coint_check >= coint_recheck_every` (D-06); otherwise carry the last re-check result for that pair (never the stale hedge_candidates.csv value — that CSV is used ONCE at startup only to build the candidate pair universe, never inside the per-bar loop; Pitfall 1). On a coint re-check returning False for an open position, ensure the exit path fires (evaluate_hedge_signal already treats correlation-breakdown as an exit; wire coint-loss to a withhold-on-entry and to exit-on-open via the same evaluate_hedge_signal contract or an explicit coint-loss close — document which, and test it).

    For each `evaluate_hedge_signal` result: if it's an open_hedge, compute `stop_distance_price_units = entry_spread_std * STOP_DISTANCE_STD_MULTIPLIER` (entry_spread_std from compute_zscore's returned std at entry) and call `risk_evaluate_fn(...)`; only open the position when the returned RiskDecision.approved is True (HEDGE-02 — the risk engine is the sole authority on whether a proposal becomes real). If it's a close_hedge, act on it (close proposals are the module's own exit authority per D-03/D-04/D-05, distinct from risk-driven closes which remain risk_engine's/Phase 4's authority). Use hedge_candidates.csv only at startup to seed the candidate pair universe, never re-read inside the loop.

    Add a feed-driven integration test to tests/test_hedge_engine.py: build a tiny synthetic replay feed (or temp parquet files) with a known cointegrated pair that diverges then reverts, run run_hedge_loop with a stub risk_evaluate_fn recording every proposal, and assert (a) an open proposal is emitted when z-score crosses the entry threshold on a cointegrated bar, (b) a reversion close proposal follows, (c) every proposal passed through the stub risk fn, (d) a risk-rejected open (stub returns approved=False) does not open a position, (e) re-running the same feed yields identical proposals (determinism). Add a coint-breakdown test: a pair that loses cointegration mid-hold triggers a withhold/exit rather than continuing to trust the original test.
  </action>
  <verify>
    <automated>python -m pytest tests/test_hedge_engine.py -q -k "loop or integration or determinism or recheck_schedule"</automated>
  </verify>
  <done>run_hedge_loop drives one feed-agnostic loop body that recalculates beta/z-score/correlation via reused backtest_engine functions, re-verifies cointegration on the D-06 schedule (and before entry), routes every proposal through the injected risk fn (HEDGE-02), withholds/exits broken-down pairs (HEDGE-03), keeps all state function-local, and is deterministic across replays; integration tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hedge_engine -> risk_engine | every proposal crosses here to be sized/approved; hedge_engine must not pre-empt or bypass this authority |
| features_*.parquet -> replay_feed | untrusted-shape parquet (possible timestamp misalignment, missing bars) crosses into the loop |
| strategy_registry params JSON -> propose_to_risk_engine | JSON text parsed at proposal time; malformed blob can crash the loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Elevation of Privilege | hedge_engine self-sizing / self-veto | high | mitigate | Executable no-self-sizing guard (Task 2) + every proposal routed through risk_engine.evaluate_order (Task 3); hedge_engine never computes lots or branches on risk_limits numeric fields (HEDGE-02) |
| T-03-06 | Tampering | stale hedge_candidates.csv used as live signal | high | mitigate | CSV used only at startup to seed the candidate universe; all z-score/correlation/cointegration values inside the loop are recomputed per bar (Task 3, Pitfall 1) |
| T-03-07 | Denial of Service | malformed strategy params JSON mid-loop | medium | mitigate | try/except around json.loads per strategy; log and skip, never crash the whole loop (Task 1, V5 Security Domain) |
| T-03-08 | Tampering | replay_feed timestamp misalignment | low | accept | Index-intersection + alignment-drop warning surfaces the problem for investigation; strict per-broker validation deferred to Phase 4 live feed (Open Question 2) |
| T-03-09 | Tampering | non-finite entry_price/stop_distance bypassing validation | medium | mitigate | Pass caller numerics through unchanged so risk_engine.validate_order_proposal's reject path fires; never clamp to fake-positive (Task 1) |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this phase (RESEARCH.md Package Legitimacy Audit: not triggered) |
</threat_model>

<verification>
- `python -m pytest tests/test_hedge_engine.py -q` — full Plan 03-01 + 03-02 suite passes.
- No-self-sizing executable guard passes (proves HEDGE-02 structurally, not just by review).
- `python -c "src=open('src/hedge_engine.py').read(); assert 'read_csv' not in src.split('def run_hedge_loop')[1] if 'def run_hedge_loop' in src else True; print('ok: no csv read inside loop region')"` — confirms hedge_candidates.csv is not read inside the loop (Pitfall 1). (Region-scoped; the CSV may still be read once at startup outside the loop.)
- Grep confirms `from backtest_engine import` (or `from src.backtest_engine import`) compute_rolling_beta and compute_zscore — reuse, not reimplementation.
</verification>

<success_criteria>
1. Every hedge proposal is routed through risk_engine.evaluate_order before it can open a position; a risk-rejected open does not open a position (HEDGE-02).
2. hedge_engine.py computes no lot size and no risk gate of its own — proven by an executable guard, not only code review (HEDGE-02).
3. run_hedge_loop is a single feed-agnostic loop body (no backtest/live fork) that recalculates beta/z-score/correlation via reused backtest_engine functions (Pitfall 5, no drift).
4. Cointegration is re-verified on the D-06 schedule and before every entry; broken-down pairs are withheld/exited, never trusted from the stale CSV snapshot (HEDGE-03).
5. Kelly inputs come only from risk_engine.resolve_kelly_inputs; the loop is deterministic across identical replays (no hidden state).
</success_criteria>

<output>
Create `.planning/phases/03-production-hedge-engine/03-02-SUMMARY.md` when done.
</output>
