---
phase: 02-deterministic-risk-engine
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/test_risk_engine.py
autonomous: true
requirements: [RISK-08, RISK-09]
user_setup: []

must_haves:
  truths:
    - "A pytest suite exercises every D-14 adversarial case against risk_engine.py and every one is rejected with the correct reject_reason."
    - "The suite proves oversized lots, negative/zero lots, invalid symbols, and duplicate orders are all rejected."
    - "The suite proves each limit (drawdown, exposure, position count) is breached-and-rejected individually and in combination."
    - "A test asserts (structurally) that risk_engine.py and risk_limits.py contain no ml_model import (RISK-09)."
  artifacts:
    - tests/test_risk_engine.py
  key_links:
    - "The suite imports the pure functions and dataclasses from src/risk_engine.py and src/risk_limits.py and drives them with plain dataclass fixtures (no mocking)."
---

<objective>
Build the RISK-08 synthetic adversarial test suite: `tests/test_risk_engine.py` (pytest). This proves the Python risk engine correctly rejects every adversarial order class from D-14 — oversized lots, duplicate orders, invalid/unknown symbols, negative/zero lots, and orders that breach each limit (drawdown, exposure, position count) individually and in combination — before any real-data use, satisfying the pre-real-money checklist in docs/risk_engine_mql5_spec.md. It also encodes the RISK-09 ML-independence static check as an executable test so the constraint is proven, not merely claimed.

Purpose: This suite is a direct instantiation of CLAUDE.md rule 6 (test exhaustively before real capital) and the spec's non-negotiable adversarial-testing checklist item.
Output: `tests/test_risk_engine.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-deterministic-risk-engine/02-CONTEXT.md
@.planning/phases/02-deterministic-risk-engine/02-RESEARCH.md
@.planning/phases/02-deterministic-risk-engine/02-PATTERNS.md
@.planning/phases/02-deterministic-risk-engine/02-01-SUMMARY.md
@src/risk_engine.py
@src/risk_limits.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the D-14 adversarial pytest suite</name>
  <files>tests/test_risk_engine.py</files>
  <action>Create tests/test_risk_engine.py (first pytest file in the repo; follow 02-PATTERNS.md guidance — plain dataclass fixtures, no mocking framework, exhaustive Portuguese docstrings). Import evaluate_order, the check_* functions, AccountState, RiskDecision from src.risk_engine and RiskLimits from src.risk_limits. Write one focused test per D-14 adversarial class, each asserting approved=False and the expected reject_reason: (1) oversized lot beyond a broker-max cap is rejected/clamped; (2) negative lot rejected; (3) zero lot rejected; (4) invalid/unknown symbol rejected; (5) duplicate order submission rejected (same idempotency key/order hash submitted twice); (6) daily drawdown breach rejected with daily_drawdown_breaker; (7) weekly drawdown breach rejected with weekly_drawdown_breaker; (8) absolute drawdown breach rejected with absolute_drawdown_kill_switch; (9) per-pair exposure >5% rejected; (10) correlation-adjusted aggregate exposure >15% rejected; (11) 4th concurrent pair rejected (max 3 per D-08); (12) kill-switch flag present rejects (use tmp_path fixture for the flag file); (13) COMBINATION case: an order simultaneously breaching drawdown AND exposure AND position count is rejected (verifying short-circuit ordering does not mask any single breach). Also add a positive-path test: a fully valid order returns approved=True with a positive sl_price and a 0.25x-Kelly lot size. Add an edge test that Kelly sizing floors at 0.0 when the strategy has non-positive edge (never a negative lot). Use pytest tmp_path for the kill-switch file so no real KILL_SWITCH.flag is left on disk.</action>
  <verify>
    <automated>python -m pytest tests/test_risk_engine.py -q</automated>
  </verify>
  <done>pytest runs green; every D-14 adversarial class has at least one passing test asserting the correct rejection, plus a passing positive-path test and a Kelly-floor edge test.</done>
</task>

<task type="auto">
  <name>Task 2: Encode RISK-09 ML-independence as an executable test</name>
  <files>tests/test_risk_engine.py</files>
  <action>Add a test test_risk_engine_has_no_ml_dependency() that reads the source text of src/risk_engine.py and src/risk_limits.py, strips comment lines, and asserts no line imports the ml_model module and no line references an ML inference variable pattern (a name containing "ml_model", or a risk branch on "confidence"/"probability" near sizing/exposure/drawdown). This makes RISK-09 a failing test if a future edit couples risk logic to ML output (Pitfall 1). The test reads files by path (Path(__file__).parent.parent / "src" / ...), asserting on non-comment content only. Note for the executor: do not write the literal ML-module import token as a plain-text search needle that also appears unstripped elsewhere in this test file — build the search token from parts (e.g. concatenation) so the test's own source does not create a false positive against itself.</action>
  <verify>
    <automated>python -m pytest tests/test_risk_engine.py::test_risk_engine_has_no_ml_dependency -q</automated>
  </verify>
  <done>test_risk_engine_has_no_ml_dependency passes, proving RISK-09 structurally via an executable test that would fail if ml_model coupling were introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adversarial test input -> risk_engine | The test suite is the adversary: it drives every malformed/limit-breaching order class across the risk-decision boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | Malformed order inputs (oversized/negative/zero lots, invalid symbols) | high | mitigate | Suite proves each is rejected at the risk_engine boundary (D-14); a regression that accepts one fails CI. |
| T-02-06 | Denial of Service | Duplicate order submission | medium | mitigate | Suite asserts duplicate (same idempotency key) is rejected. |
| T-02-07 | Elevation of Privilege | Silent future ML coupling in risk logic | high | mitigate | Executable RISK-09 test fails if any ml_model import/ML-branch is introduced. |
</threat_model>

<verification>
- `python -m pytest tests/test_risk_engine.py -q` passes with every D-14 adversarial class covered.
- The RISK-09 executable test fails if ml_model coupling is introduced (proven by construction).
</verification>

<success_criteria>
1. Every D-14 adversarial class (oversized/negative/zero lot, invalid symbol, duplicate order, each limit breach individually and combined) has a passing rejection test (RISK-08).
2. A positive-path test confirms a valid order is approved with a positive SL and 0.25x-Kelly size.
3. An executable RISK-09 test proves no ml_model coupling in the risk source (RISK-09).
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-risk-engine/02-02-SUMMARY.md` when done.
</output>
