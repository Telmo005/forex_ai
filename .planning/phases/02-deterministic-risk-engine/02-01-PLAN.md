---
phase: 02-deterministic-risk-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/risk_limits.py
  - src/risk_engine.py
autonomous: true
requirements: [RISK-01, RISK-02, RISK-03, RISK-04, RISK-05, RISK-06, RISK-09]
user_setup: []

must_haves:
  truths:
    - "Every order evaluated by risk_engine.py returns a deterministic stop-loss price and a Kelly-fractional (0.25x) lot size, or an explicit rejection reason."
    - "An order that breaches per-pair (5%) or correlation-adjusted aggregate (15%) exposure is rejected."
    - "An order is blocked when a daily (3%), weekly (8%), or absolute (20%) drawdown breaker is active, or when >3 concurrent hedge pairs are open."
    - "Creating KILL_SWITCH.flag causes the next evaluate_order() call to reject with a kill_switch reason."
    - "risk_engine.py and risk_limits.py contain no import of, or branch on, ml_model.py or any ML inference path."
  artifacts:
    - src/risk_limits.py
    - src/risk_engine.py
  key_links:
    - "risk_engine.py reads Kelly inputs (win_rate, payoff_ratio) from strategy_registry.get_strategy(), preferring walk-forward out-of-sample stats (wf_passed=1 and revalidated_on_real_data=1) over raw backtest stats."
    - "All D-01..D-14 numeric limits live in risk_limits.py as named constants / RiskLimits dataclass fields, each citing its decision ID."
    - "D-13: evaluate_order()'s RiskDecision return value uses only JSON-serializable field types (bool/float/str/None) so it can be written to the file-based Python<->MQL5 bridge Phase 3/4 will build, without requiring any interface change when that bridge is added."
---

<objective>
Build the Python pre-trade risk authority: `src/risk_limits.py` (named constant tables mirroring `backtest_engine.py`'s convention) and `src/risk_engine.py` (pure-function risk-decision core). This delivers deterministic stop-loss attachment (RISK-01), fractional-Kelly position sizing at 0.25x per D-01 (RISK-02), per-pair and correlation-adjusted aggregate exposure limits per D-06/D-07 (RISK-03), daily/weekly/absolute drawdown circuit breakers per D-03/D-04/D-05 (RISK-04), max-concurrent-position cap per D-08 (RISK-05), and the file-based kill-switch checked Python-side per D-09 (RISK-06). Every risk decision is a pure function with explicit inputs and no hidden I/O, and no code path imports or branches on `ml_model.py` (RISK-09).

Purpose: This is the single most safety-critical module in the system — the deterministic gate that no order can bypass before reaching execution.
Output: `src/risk_limits.py`, `src/risk_engine.py`.

Alerting scope (DEFERRED — do NOT build in this phase): D-10 (Telegram alert channel) and D-11 (80%-of-limit alert threshold), both from 02-CONTEXT.md, are NOT implemented in Phase 2. Per REQUIREMENTS.md traceability, the alerting requirement ALERT-01 is mapped to **Phase 4**, not Phase 2 — Phase 2's requirements are RISK-01 through RISK-09 only, none of which mention alerting. Building Telegram alerting here would be scope creep into Phase 4's requirement. Therefore: create NO `alerts.py` file, add NO `python-telegram-bot`/`requests`-based alert sender, and import NO alerting library in this phase. What Phase 2 DOES do is expose the state Phase 4's alerting will consume: `risk_engine.py` must return, as clean queryable values on its decision/state objects, the current drawdown percentage relative to each limit (daily/weekly/absolute) — so Phase 4 can later compute "80% of nearest limit" without Phase 2 owning any alert logic. Phase 2 exposes the numbers; Phase 4 (ALERT-01) decides and sends alerts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deterministic-risk-engine/02-CONTEXT.md
@.planning/phases/02-deterministic-risk-engine/02-RESEARCH.md
@.planning/phases/02-deterministic-risk-engine/02-PATTERNS.md
@docs/risk_engine_mql5_spec.md
@.claude/skills/quant-finance-math/SKILL.md

# Analog files — copy the named-constant-table and pure-function conventions:
@src/backtest_engine.py
@src/strategy_registry.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/risk_limits.py constant tables</name>
  <files>src/risk_limits.py</files>
  <behavior>
    - RiskLimits dataclass instantiates with the exact locked defaults and each field cites its decision ID in an inline comment.
    - Reading each constant back returns the D-locked value: kelly_fraction=0.25, max_pair_exposure_pct=0.05, max_aggregate_exposure_pct=0.15, daily_drawdown_pct=0.03, weekly_drawdown_pct=0.08, absolute_drawdown_pct=0.20, max_concurrent_pairs=3, alert_threshold_pct_of_limit=0.80, heartbeat_timeout_seconds=30.
    - KILL_SWITCH_PATH constant is defined and overridable (a parameter default, not hard-wired into logic).
  </behavior>
  <action>Create src/risk_limits.py following the named-constant-table convention from backtest_engine.py's DEFAULT_COST_PARAMS/WALK_FORWARD_CONFIG (see 02-PATTERNS.md, analog excerpt). Define a RiskLimits dataclass (mirroring PipelineConfig style) with fields: kelly_fraction=0.25 per D-01, max_pair_exposure_pct=0.05 per D-06, max_aggregate_exposure_pct=0.15 per D-07, daily_drawdown_pct=0.03 per D-03, weekly_drawdown_pct=0.08 per D-04, absolute_drawdown_pct=0.20 per D-05, max_concurrent_pairs=3 per D-08 (3 hedge pairs / 6 legs), alert_threshold_pct_of_limit=0.80 per D-11 (data-only named constant exposed for Phase 4's ALERT-01 to consume — Phase 2 defines the number but implements NO alerting logic and adds NO alerts.py, per the deferral note in the objective), heartbeat_timeout_seconds=30 per D-12. Every field carries an inline comment naming its decision ID and citing 02-CONTEXT.md as provenance — no bare magic numbers. Define KILL_SWITCH_PATH default constant per D-09. Write the module docstring in Portuguese (project convention) explaining these are retunable named constants, not magic numbers, and that they are duplicated by hand in RiskGuard.mqh (never shared via import — that is impossible across languages). Import only `from __future__ import annotations` and `from dataclasses import dataclass`; do NOT import numpy, pandas, or ml_model. Choose the variance-scaling correlation approach for D-07 (see Task 2) and document the CORRELATION_ADJUSTMENT approach name as a constant here.</action>
  <verify>
    <automated>python -c "from src.risk_limits import RiskLimits; r=RiskLimits(); assert (r.kelly_fraction,r.max_pair_exposure_pct,r.max_aggregate_exposure_pct,r.daily_drawdown_pct,r.weekly_drawdown_pct,r.absolute_drawdown_pct,r.max_concurrent_pairs)==(0.25,0.05,0.15,0.03,0.08,0.20,3), 'locked D-values wrong'; print('risk_limits OK')"</automated>
  </verify>
  <done>RiskLimits dataclass exists with all D-01..D-14 numeric limits as named fields citing decision IDs; the verify command prints "risk_limits OK".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/risk_engine.py pure-function decision core</name>
  <files>src/risk_engine.py</files>
  <behavior>
    - check_kill_switch(path) returns (False, "kill_switch") when the flag file exists, (True, None) otherwise.
    - check_drawdown_breaker returns (False, "absolute_drawdown_kill_switch") at >=20% from HWM, (False, "daily_drawdown_breaker") at >=3% intraday, (False, "weekly_drawdown_breaker") at >=8% intraweek, (True, None) otherwise.
    - check_position_count rejects when open pairs >= 3 (attempting a 4th).
    - check_exposure_limits rejects when any single pair >5% or the correlation-adjusted aggregate >15%.
    - size_position returns a lot size derived from kelly_fraction(win_rate, payoff_ratio, 0.25); returns 0.0 when edge is non-positive (never negative).
    - evaluate_order aggregates all checks in kill-switch -> drawdown -> position-count -> exposure -> sizing order and returns a RiskDecision {approved, size_lots, sl_price, reject_reason}; a rejection short-circuits with a non-null reject_reason and approved=False.
  </behavior>
  <action>Create src/risk_engine.py as a pure-function decision core (see 02-RESEARCH.md Pattern 1 and 02-PATTERNS.md). Define value-object dataclasses AccountState (equity, daily_start_equity, weekly_start_equity, absolute_hwm, open_positions: list[dict]) and RiskDecision (approved: bool, size_lots: float, sl_price: float, reject_reason: str | None). Implement these pure functions, each returning tuple[bool, str | None] mirroring validate_strategy's shape (02-PATTERNS.md): check_kill_switch(path) using os.path.exists per D-09; check_drawdown_breaker(state, limits) per D-03/D-04/D-05 with absolute checked first (it is a standing kill condition per D-05, requires manual reset); additionally expose, as a clean queryable value (e.g. a drawdown_pct_of_limit(state, limits) accessor returning current daily/weekly/absolute drawdown as a fraction of each respective limit, or those fractions carried on RiskDecision/AccountState), the current drawdown percentage relative to each limit — this is the state Phase 4's ALERT-01 will consume to compute its 80%-of-nearest-limit trigger (D-11). Phase 2 exposes these numbers only; it must NOT import any alerting library, send any alert, or add alerts.py (see objective deferral note); check_position_count(state, limits) per D-08; check_exposure_limits(state, correlation_matrix, limits) per D-06/D-07. For D-07 use the variance-scaling approach: aggregate = raw_sum * (1 + avg_pairwise_correlation), degrading to raw_sum for <2 positions (02-RESEARCH.md Code Examples, Open Question 1 recommendation) — name the approach in a comment and cite 02-RESEARCH.md. Implement kelly-based size_position() by calling a kelly_fraction(win_rate, payoff_ratio, fraction=0.25) helper implemented verbatim from the quant-finance-math skill (f_star=(b*p-q)/b, floored at 0.0) with fraction=0.25 per D-01 — do NOT re-derive the formula. Read Kelly inputs via strategy_registry.get_strategy() and apply the Pitfall 4 out-of-sample gate: prefer wf_fold_results aggregate stats when wf_passed=1 and revalidated_on_real_data=1, else fall back to raw win_rate/profit_factor with an explicit log.warning. Compute a deterministic stop-loss price (RISK-01) as part of the decision — the order is never approved without a positive sl_price. Compose everything in evaluate_order(proposal, state, limits, correlation_matrix) returning a RiskDecision; ordering: kill-switch, then drawdown, then position count, then exposure, then sizing+SL. The kill-switch and drawdown breakers block NEW orders only — they must NEVER trigger a close-all/liquidate action (Pitfall 5, RISK-06 semantics). CRITICAL: this file must not import ml_model or any ML module, and must not branch on any variable named confidence/signal/probability/ml_* (RISK-09, Pitfall 1). Use logging per project convention (log = logging.getLogger("risk_engine")). Write exhaustive Portuguese docstrings documenting params/returns/raises for each function.</action>
  <verify>
    <automated>python -c "import os,tempfile; from src.risk_engine import evaluate_order, AccountState; from src.risk_limits import RiskLimits; import src.risk_engine as re; ok,reason=re.check_drawdown_breaker(AccountState(equity=8000,daily_start_equity=9800,weekly_start_equity=9500,absolute_hwm=10000,open_positions=[]), RiskLimits()); assert (ok,reason)==(False,'absolute_drawdown_kill_switch'), reason; print('risk_engine drawdown OK')"</automated>
  </verify>
  <done>All five check_* functions plus size_position and evaluate_order exist and are pure (no hidden I/O in decision logic); drawdown/exposure/position/kill-switch/Kelly behaviors match the D-locked values; the verify command prints "risk_engine drawdown OK".</done>
</task>

<task type="auto">
  <name>Task 3: Add ML-independence static gate assertion to Python side</name>
  <files>src/risk_engine.py, src/risk_limits.py</files>
  <action>Verify structurally (not just by docstring) that neither risk_engine.py nor risk_limits.py imports or references ml_model (RISK-09, per RESEARCH.md Pitfall 1 which mandates a static grep-based check as part of THIS phase's verification, not a deferred lint pass). Confirm no line matches an import of the ML module and no branch references an ML inference variable. If any reference exists, remove it. This task adds no new ml_model references; the verify gate below is the structural proof required by RISK-09. Note for the executor: the grep gate greps source lines excluding comments, so do not write the literal ML-module import token anywhere in these two files, including in comments or docstrings.</action>
  <verify>
    <automated>test $(grep -v '^\s*#' src/risk_engine.py src/risk_limits.py | grep -cE 'import[[:space:]]+ml_model|from[[:space:]]+ml_model') -eq 0 && echo "RISK-09 python static gate PASS"</automated>
  </verify>
  <done>Grep of non-comment lines in risk_engine.py and risk_limits.py finds zero imports of ml_model; the verify command prints "RISK-09 python static gate PASS".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hedge_engine (future) -> risk_engine | Proposed order (symbol, lots, win_rate, payoff_ratio) crosses into the risk decision core — untrusted, must be validated. |
| filesystem -> risk_engine | KILL_SWITCH.flag presence and strategy_registry SQLite reads cross into the decision path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | evaluate_order input (lots, symbol, prices) | high | mitigate | Validate lot size positive/finite, symbol against known list, prices non-NaN/non-negative before sizing; adversarial suite (Plan 02) proves rejection. |
| T-02-02 | Elevation of Privilege | Kelly sizing coupling to ML output | high | mitigate | RISK-09 static grep gate (Task 3) proves no ml_model import; Kelly inputs frozen from strategy_registry, never live model output (D-02). |
| T-02-03 | Denial of Service | Kill-switch false-absence | medium | mitigate | Kill-switch path is a documented named constant so it is not swept by generic cleanup; presence-based check re-run every evaluate_order call. |
| T-02-04 | Tampering | Kill-switch reinterpreted as close-all | high | mitigate | Kill-switch/drawdown block NEW orders only, never liquidate (Pitfall 5); enforced in evaluate_order ordering. |
</threat_model>

<verification>
- `python -c "from src.risk_engine import evaluate_order, AccountState, RiskDecision; from src.risk_limits import RiskLimits"` imports cleanly.
- RISK-09 static gate: grep of non-comment lines finds no ml_model import in either file.
- Kelly sizing floors at 0.0 for non-positive edge (verified in Plan 02 suite).
</verification>

<success_criteria>
1. risk_engine.py evaluate_order returns a RiskDecision with a positive sl_price and a 0.25x-Kelly lot size for a valid order, or an explicit reject_reason otherwise (RISK-01, RISK-02).
2. Exposure (RISK-03), drawdown (RISK-04), position-count (RISK-05), and kill-switch (RISK-06) checks each reject when their D-locked limit is breached.
3. All numeric limits are named constants citing decision IDs; zero magic numbers.
4. Static grep confirms no ml_model import or ML-branch in either file (RISK-09).
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-risk-engine/02-01-SUMMARY.md` when done.
</output>
